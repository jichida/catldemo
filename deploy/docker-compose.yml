version: '2'
services:
public:
  image: nginx
  restart: always
  ports:
      - "8080:8080"
  volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./dist:/usr/share/nginx/html
